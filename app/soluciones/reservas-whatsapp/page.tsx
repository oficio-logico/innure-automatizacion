import { SolutionDetail, solutionMetadata } from '../../solution-views';
export const dynamic = 'force-static';
export const metadata = solutionMetadata('reservas-whatsapp');
export default function Page() { return <SolutionDetail slug="reservas-whatsapp" />; }
